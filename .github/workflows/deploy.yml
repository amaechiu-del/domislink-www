name: Disabled Cloudflare Pages deploy

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    if: false
    steps:
      - name: Disabled
        run: echo "Cloudflare Pages deploy disabled. Use GitHub Pages workflow instead." 
